import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TraderStatsComponent } from './trader-stats.component';

describe('TraderStatsComponent', () => {
  let component: TraderStatsComponent;
  let fixture: ComponentFixture<TraderStatsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TraderStatsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TraderStatsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
